name: Deploy Ecosystem Tracker

on:
  push:
    branches: [main]
    paths:
      - 'ecosystem-tracker/backend/**'
  workflow_dispatch:

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: ecosystem-tracker/backend
        run: |
          pip install -r requirements.txt

      - name: Initialize database
        working-directory: ecosystem-tracker/backend
        run: |
          python init_db.py

      - name: Run tests (placeholder)
        working-directory: ecosystem-tracker/backend
        run: |
          echo "测试占位：后续添加单元测试"
          # pytest tests/ -v

      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /home/manue1/hermes_project/projects/ecosystem-tracker
            git pull origin main
            cd backend
            pip install -r requirements.txt
            # 重启 systemd 服务
            sudo systemctl restart ecosystem-tracker || true
            echo "后端部署完成"

  # 微信小程序需要手动上传，CI 仅做构建验证
  # verify-miniprogram:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Verify mini-program structure
  #       run: |
  #         echo "小程序项目结构验证"
  #         ls -la ecosystem-tracker/frontend/
